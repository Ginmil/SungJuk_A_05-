```mermaid
sequenceDiagram
    autonumber
    actor 교수님 as 교수 (Actor)
    participant UI as Search_Sungjuk_UI
    participant 교 as 교수
    participant 성 as 성적

    교수님->>+UI: 학점 조회 요청 (교수id, 학생id)
    UI->>+교: 교수체크(교수id:String) 호출
    교-->>-UI: 체크 결과 반환 (boolean)

    alt 교수id == "inha" (성공)
        UI->>+성: 학점조회(학생id:String) 호출
        Note over 성: 점수 기반 학점 부여<br/>(A: 90이상, B: 80이상, C: 80미만)
        성-->>-UI: 부여된 학점 결과 반환
        UI-->>교수님: 학점 결과 텍스트 출력
    else 교수id != "inha" (실패)
        UI-->>-교수님: "교수 인증 실패" 메시지 출력
    end