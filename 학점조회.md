```mermaid
sequenceDiagram
    autonumber
    actor Prof as 교수 (Actor)
    participant UI as Search_Sungjuk_UI
    participant P as 교수
    participant S as 성적

    Prof->>+UI: 학점조회_화면() 실행
    UI->>+P: 교수체크(id) 호출
    P-->>-UI: 결과 반환 (boolean)

    alt 체크 성공 (true)
        UI->>+S: 학점조회(교수id) 호출
        S-->>-UI: 학점 결과(String) 반환
        UI-->>Prof: 학점 결과 출력
    else 체크 실패 (false)
        UI-->>-Prof: "권한이 없습니다" 출력
    end