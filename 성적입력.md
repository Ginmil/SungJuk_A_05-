```mermaid
sequenceDiagram
    autonumber
    actor 교수님 as 교수 (Actor)
    participant UI as Add_Sungjuk_UI
    participant 교 as 교수
    participant 성 as 성적

    교수님->>+UI: 성적 입력 요청 (교수id, 학생id, 과목성적들)
    UI->>+교: 교수체크(교수id:String) 호출
    교-->>-UI: 체크 결과 반환 (boolean)

    alt 교수id == "inha" (성공)
        UI->>+성: 성적입력(학생id:String, 자바:int, DB:int, 보안:int) 호출
        Note over 성: 총점 및 평균 계산 수행
        성-->>-UI: 입력 성공 및 계산 결과 반환
        UI-->>교수님: 처리 결과 텍스트 출력
    else 교수id != "inha" (실패)
        UI-->>-교수님: "교수 인증 실패" 메시지 출력
    end