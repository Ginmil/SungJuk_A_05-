```mermaid
sequenceDiagram
    autonumber
    actor Prof as 교수 (Actor)
    participant UI as Add_Sungjuk_UI
    participant P as 교수
    participant S as 성적

    Prof->>+UI: 성적입력_화면() 실행
    UI->>+P: 교수체크(id) 호출
    P-->>-UI: 결과 반환 (boolean)

    alt 체크 성공 (true)
        UI->>+S: 성적입력(교수id, 학생id, 자바, DB, 보안) 호출
        Note over S: 총점/평균 계산 및 데이터 저장
        S-->>-UI: 입력 완료 응답
        UI-->>Prof: "성적 입력 성공" 출력
    else 체크 실패 (false)
        UI-->>-Prof: "권한이 없습니다" 출력
    end