package Score;

public class 교수 {
    /**
     * 교수id를 체크한다.
     * @param 교수id 인증할 아이디
     * @return "inha"일 경우 true 반환
     */
    public boolean 교수체크(String 교수id) {
        return "inha".equals(교수id);
    }
}